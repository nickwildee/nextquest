# NextQuest 개발 계획

## 개발 원칙

- 제품의 핵심 가치를 외부 연동보다 먼저 검증한다.
- YAGNI 원칙에 따라 현재 사용자 흐름에 필요하지 않은 기능을 미리 만들지 않는다.
- KISS 원칙에 따라 검수된 소수 게임과 단순한 상태 모델로 시작한다.
- DRY 원칙은 실제 중복이 확인될 때 적용하고, 예상만으로 추상화를 만들지 않는다.
- Steam, 리뷰 수집, AI 분석 문제를 한 번에 결합하지 않고 단계별로 교체한다.
- M0에서는 기술 스택, 데이터베이스, API, 세부 아키텍처를 고정하지 않는다.

## M0: 제품 정의 및 MVP 범위 확정

### 목적

구현 전에 해결할 문제, 대상 사용자, 핵심 가치, MVP 범위와 대표 사용자 흐름을 합의한다.

### 결과물

- `docs/project-context.md`
- `docs/product/scope.md`
- `docs/product/user-flow.md`
- `docs/product/development-plan.md`
- 핵심 문서로 연결되는 `README.md`

### 완료 조건

- 해결할 문제와 핵심 사용자를 한 문장으로 설명할 수 있다.
- MVP 포함 범위와 제외 범위가 구분되어 있다.
- 대표 사용자 흐름이 서비스 진입부터 구매 판단 저장까지 연결된다.
- 첫 개발 마일스톤의 우선순위와 완료 기준이 정의되어 있다.
- README에서 모든 핵심 문서로 이동할 수 있다.
- 문서 간 내용과 링크를 검증하고 사용자의 최종 확인을 받는다.

## M1: 개발 기반 구축

### 목적

M2의 대표 사용자 흐름을 외부 서비스 없이 안전하게 구현하고 반복 검증할 수 있도록 기술 스택, 프로젝트 구조와 개발 하네스를 확정한다.

### 기술 경계

- pnpm workspace 기반 모노레포에서 `apps/web`을 Next.js 애플리케이션으로 구성한다.
- 웹에 필요한 서버 기능은 Next.js Route Handler로 시작하며 M1에는 별도 API 애플리케이션을 만들지 않는다.
- `packages/domain`에는 프레임워크와 데이터베이스에 의존하지 않는 도메인 스키마, 규칙과 fixture를 둔다.
- 독립 실행·배포, 여러 클라이언트 또는 백그라운드 처리 요구가 확인되면 서버 기능을 별도 API 애플리케이션으로 분리한다.

목표 workspace 구조와 의존 방향은 다음과 같다.

```text
pnpm workspace
├── apps/web
│   └── packages/domain을 사용
└── packages/domain
    └── React, Next.js, HTTP와 데이터베이스에 의존하지 않음
```

`apps/web`과 `packages/domain`은 workspace의 형제이며 의존 방향은 `apps/web → packages/domain` 한 방향이다.

### 검증 경계

- ESLint는 코드 품질, Prettier는 코드 형식, TypeScript strict 모드는 타입 검사를 책임진다.
- Zod로 fixture와 API 경계의 스키마를 검증하고 Jest로 도메인 규칙, golden fixture와 독립적인 Client Component를 테스트한다.
- Storybook과 MSW로 컴포넌트 상태를 재현하고 Storybook Vitest addon과 a11y addon으로 story 렌더링, 상호작용과 접근성을 검증한다.
- Playwright는 대표 사용자 흐름과 실패 trace, GitHub Actions는 PR의 공통 검증 실행을 책임진다.
- `*.test.ts(x)`는 Jest, `*.stories.tsx`는 Storybook Vitest addon, `e2e/*.spec.ts`는 Playwright가 실행한다.
- 합성 fixture는 `packages/domain`이 소유한다. 순수 도메인 테스트는 fixture를 직접 사용하고, 공용 MSW handler는 로컬 개발, Storybook과 HTTP 경계가 필요한 테스트 환경에서 같은 fixture를 응답으로 제공한다.
- 환경별 MSW 시작 코드는 분리하며 production 빌드에서는 worker를 시작하지 않는다.

### 우선순위

| 순서 | 결과 | 담당 Issue | 선행 작업 |
| ---: | --- | --- | --- |
| 1 | 기술 스택, 패키지 매니저와 프로젝트 구조 결정 | [#8](https://github.com/nickwildee/nextquest/issues/8) | 없음 |
| 2 | pnpm·Next.js 스캐폴딩과 Jest 기반의 빠른 내부 검증 루프 | [#11](https://github.com/nickwildee/nextquest/issues/11) | #8 |
| 3 | 도메인 스키마와 golden fixture를 사용한 핵심 규칙 검증 | [#12](https://github.com/nickwildee/nextquest/issues/12) | #11, 상위 영역·하위 특성 분류표 확정 |
| 4 | Storybook·MSW를 사용한 컴포넌트 상태와 API mock 경계 | [#18](https://github.com/nickwildee/nextquest/issues/18) | #11, #12 |
| 5 | Playwright smoke test와 GitHub Actions 외부 검증 루프 | [#19](https://github.com/nickwildee/nextquest/issues/19) | #11, #12, #18 |

### 완료 조건

- 새 환경에서 하나의 패키지 매니저로 의존성을 설치하고 프로젝트를 실행할 수 있다.
- 개발 서버와 공통 검증 절차를 저장소에 정의된 명령으로 실행할 수 있다.
- format, lint, typecheck와 단위·컴포넌트 테스트를 독립적으로 실행할 수 있다.
- 고정된 입력과 기대 결과로 핵심 도메인 규칙을 검증할 수 있다.
- 같은 API mock과 fixture를 로컬 개발, 컴포넌트 상태 확인과 테스트에서 재사용할 수 있다.
- 로딩, 빈 결과, 오류와 비활성 상태를 실제 외부 서비스 없이 확인할 수 있다.
- 최소 브라우저 smoke test와 CI가 공통 검증 명령을 실행한다.
- 기술 선택과 후속 구현 범위가 관련 Issue에 기록되어 있다.

### M1에서 하지 않는 일

- fixture 기반 대표 사용자 흐름 전체 구현
- 별도 API 애플리케이션과 서버 프레임워크 구성
- 실제 Steam 계정 연동과 사용자 인증
- 실제 리뷰 수집과 온라인 AI 분석 호출
- 데이터베이스와 운영 인프라 구축
- 애플리케이션 컨테이너와 PostgreSQL Compose 구성
- 8개 게임 전체의 실제 분석 데이터 작성

## M2: fixture 기반 핵심 사용자 흐름

fixture는 실제 API 대신 화면과 로직 검증에 사용하는 고정 데이터다. 정상 화면의 리뷰 근거는 출처와 이용 조건을 확인한 최소 실제 문장을 수동으로 준비하고, 자동 테스트의 오류·빈 결과·충돌 사례는 합성 문장을 사용한다. M2에서는 Steam 연동, 자동 리뷰 수집, 사용자 요청 시 온라인 AI 호출 없이 핵심 흐름을 완성한다.

### 우선순위

1. 로그인 없는 기본 리뷰 분석
2. fixture 로그인과 개인화 접근 제어
3. 게임별 플레이 시간 조건과 충분한 경험 자기 확인
4. 비교 기준 게임 3개 이상에서 모든 상위 경험 영역 확인
5. 영역별 중요 하위 특성 최대 3개의 `좋았음`·`아쉬웠음` 입력과 `경험 부족` 처리
6. 모든 유효 비교 게임을 사용한 하위 특성별 개인화 비교
7. `맞을 근거`, `주의 신호`, `판단 불가`와 동시 존재 처리
8. 실제 리뷰 근거 3개와 더보기 최대 10개
9. `구매 후보`, `보류`, `제외`와 과거 평가 수정
10. 초안·저장 상태의 `localStorage` 복구

### fixture 검증 범위

1. 분석 대상 후보 게임 한 개와 개인화에 필요한 비교 게임 3개 이상의 fixture로 대표 흐름을 먼저 연결한다.
2. 분석 대상 후보 게임을 2~3개로 늘려 경험 분류와 결과 구조가 특정 게임에만 맞춰지지 않았는지 확인한다.
3. 구조의 재사용 가능성을 확인한 뒤 실제 데이터 파이프라인과 8개 분석 대상 게임을 별도 범위로 확장한다.

### 완료 조건

- 사용자가 fixture 데이터만으로 기본 분석부터 구매 상태 저장까지 대표 흐름을 완료할 수 있다.
- 개인화 입력 전에 fixture 로그인을 요구한다.
- 게임별 플레이 시간과 충분한 경험 확인을 모두 충족한 비교 게임 3개 이상이 있어야 개인화를 시작한다.
- 각 비교 게임의 모든 상위 경험 영역을 확인하고, 경험한 영역에서는 중요 하위 특성을 최대 3개까지 골라 `좋았음` 또는 `아쉬웠음`을 표시할 수 있다.
- 같은 상위 영역에 좋아한 특성과 아쉬웠던 특성을 함께 저장할 수 있다.
- `경험 부족`은 상위 영역 확인 완료에 포함되지만 해당 영역의 취향 계산에서 제외된다.
- 확인하지 않은 영역이나 하위 특성이 없는 경험 영역이 있으면 입력을 완료할 수 없다.
- 조건을 충족하고 평가를 완료한 모든 비교 게임이 개인화에 사용된다.
- 개인화 결과가 `맞을 근거`, `주의 신호`, `판단 불가`로 구분된다.
- 같은 상위 영역에 `맞을 근거`와 `주의 신호`가 동시에 있는 fixture를 처리한다.
- 비교 가능한 특성이나 리뷰 근거가 부족하거나 충돌하면 하위 특성별 `판단 불가`를 제공한다.
- 모든 노출 분석 결과에 fixture 리뷰 근거가 연결된다.
- 각 결과에서 근거 3개를 먼저 보고 최대 10개까지 펼칠 수 있다.
- 비교 기준 게임이 3개 미만인 사용자는 기본 분석을 계속 이용할 수 있다.
- 과거 경험 평가를 수정하면 개인화 결과가 최신 입력을 반영한다.
- 작성 중인 입력과 저장된 상태가 새로고침 후 `localStorage`에서 복구된다.
- 종합 적합도 점수가 화면이나 로직에 포함되지 않는다.
- 대표 흐름과 주요 예외 흐름에 대한 E2E 테스트가 통과한다.
- 핵심 사용자 한 명 이상이 fixture 흐름을 사용하고 결과 이해와 구매 판단 영향에 대한 피드백을 제공한다.

### M2에서 하지 않는 일

- 실제 Steam 계정 연동
- 실제 사용자 인증과 서버 세션
- 실제 리뷰 수집과 갱신
- 온라인 AI 분석 호출
- 서버 저장, SQLite, 데이터베이스와 운영 인프라
- 계정 기반 다른 기기 동기화
- 8개 게임 전체의 실제 분석 데이터 완성

## M3: 실제 데이터 기반 MVP 완성 및 안정화

### 목적

M2에서 검증한 대표 사용자 흐름을 실제 검수 데이터와 사용자별 상태에 연결하고, 오류와 빈 상태, 입력 검증, 접근성, 테스트와 UX를 보완해 베타 사용자가 판단에 활용할 수 있는 품질로 완성한다.

### 우선순위

1. 공식 문서와 개인정보 없는 실제 응답을 기준으로 Steam 외부 스키마를 확정
2. 리뷰 특성 분류표, 표본 안정화와 의견 우세 판정 기준 검증
3. 사람 정답 데이터로 LLM 분류·번역 평가와 비용 상한 검증
4. 배치·캐시·수동 검수 기반 오프라인 리뷰 처리 파이프라인 구축
5. 분석 대상 후보 게임 한 개에서 오프라인 파이프라인 결과로 수동 fixture를 교체
6. 2~3개 후보 게임으로 데이터 구조와 검수 절차의 재사용성 확인
7. 8개 분석 대상 게임의 검수 데이터 적용
8. Steam 라이브러리 연동과 수동 등록 경로 연결
9. 확정된 인증·저장 정책에 따른 사용자별 구매 상태와 과거 경험 저장
10. 주요 오류·빈 상태, 입력 검증, 접근성, 자동 검증과 UX 안정화

### 완료 조건

- 로그인하지 않은 사용자가 8개 게임의 실제 기본 리뷰 분석과 근거를 확인할 수 있다.
- 사용자가 Steam 연동 또는 수동 등록 경로로 과거 게임을 등록할 수 있다.
- 개인화 입력, 결과와 구매 판단 상태가 확정된 저장 정책에 따라 유지된다.
- 모든 노출 분석과 개인화 결과가 검수된 실제 리뷰 근거를 참조한다.
- 분석 범위와 요청 조건에 맞는 전체 리뷰 수가 구분되어 있다.
- 성공한 리뷰 배치가 캐시되며 실패한 한 배치 때문에 완료된 배치를 다시 호출하지 않는다.
- LLM 호출 전에 예상 최대 비용을 확인하고 설정한 상한을 넘으면 자동 중단한다.
- 대표 흐름과 주요 예외 흐름에서 발견된 치명적인 오류가 해결되어 있다.
- 사용자 입력의 유효성 검증과 복구 방법이 화면에 제공된다.
- 키보드 조작, 연결된 레이블과 자연스러운 포커스 이동을 확인할 수 있다.
- 자동 검증과 브라우저 검증이 안정적으로 통과한다.
- 핵심 사용자가 기본 분석과 개인화 비교 결과를 이해할 수 있다.

## M4: 베타 배포

### 목적

검증 가능한 베타 환경에 서비스를 배포하고 동작 상태와 사용자 피드백을 확인할 수 있는 운영 경계를 준비한다.

### 완료 조건

- 저장소의 검증 절차를 통과한 버전을 베타 환경에 배포할 수 있다.
- 8개 분석 대상 게임과 실제 리뷰 근거를 베타 환경에서 확인할 수 있다.
- 배포 상태와 핵심 오류를 확인할 수 있다.
- 소울라이크 경험자 3명 이상에게 사용자 가치 검증 기준에 따른 피드백을 수집할 수 있다.
- 확인된 문제를 후속 Issue로 연결할 수 있다.

## 주요 위험과 대응 방향

| 위험 | 영향 | 대응 방향 |
| --- | --- | --- |
| Steam 라이브러리 비공개 또는 새 계정 | 자동 온보딩 실패 | Steam 등록 게임의 수동 등록 경로 제공 |
| 비교 기준 게임 부족 | 개인화에 필요한 취향 범위를 확보하지 못함 | 기본 분석은 유지하고 3개까지 필요한 게임 수와 조건을 안내 |
| 리뷰 표본 편향 | 일부 의견을 전체 여론처럼 표현 | API 전체 모수와 분석 표본을 분리하고 특성별 분모를 공개 |
| 리뷰 구조화 오류 | 잘못된 특성을 사용자 취향과 비교 | 통제된 분류표, 원문 범위, 사람 정답 데이터와 직접 검수 사용 |
| 관련 리뷰가 부족하거나 충돌 | 과도한 확신 제공 | 하위 특성별 `판단 불가`로 표시하고 종합 점수를 제공하지 않음 |
| 관련 리뷰를 찾지 못함 | 근거 없는 개인화 결과 생성 | 해당 하위 특성을 `판단 불가`로 표시하고 부족한 근거를 안내 |
| 과거 경험 평가 수정 | 이전 취향을 반영한 결과가 남음 | 개인화 결과를 최신 입력으로 다시 계산 |
| 지원 게임 범위 확대 | 검수 품질 저하와 일정 지연 | 8개 게임으로 시작하고 검증 후 확대 |
| 외부 데이터 출처 확대 | 크롤링·정규화·유지보수 증가 | MVP는 Steam 공식 정보와 Steam 리뷰로 제한 |

## 검증 순서

1. 문서에서 제품 범위와 용어의 일관성을 확인한다.
2. 분석 대상 후보 게임 한 개와 비교 게임 3개 이상의 fixture로 대표 사용자 흐름과 예외 흐름을 E2E 테스트한다.
3. 2~3개 후보 게임으로 확장해 경험 분류와 결과 구조의 재사용 가능성을 확인한다.
4. 핵심 사용자에게 기본 분석과 개인화 비교의 이해 가능성을 확인한다.
5. 제품 가치가 확인된 뒤 Steam과 실제 리뷰 파이프라인, 8개 분석 대상 게임을 연결한다.

## 관련 문서

- [프로젝트 컨텍스트](../project-context.md)
- [MVP 범위](scope.md)
- [대표 사용자 흐름](user-flow.md)
- [리뷰 분석 정책](review-analysis.md)
- [Steam API 계약](../technical/steam-api-contract.md)
- [M1 개발 기반과 검증 도구 결정 기록](https://github.com/nickwildee/nextquest/discussions/9)
- [리뷰 분석과 fixture MVP 데이터 정책 결정 기록](https://github.com/nickwildee/nextquest/discussions/21)
