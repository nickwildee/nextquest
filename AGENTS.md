# NextQuest 작업 원칙

이 문서는 NextQuest 저장소에서 작업하는 에이전트가 따라야 할 프로젝트별 기준을 정의한다.

## 작업 전 확인

- 구현이나 문서 작성을 시작하기 전에 `AGENTS.md`, 관련 이슈, 기존 문서, 현재 브랜치와 작업 트리 상태를 확인한다.
- 제품 작업은 `문제와 목적 → MVP 범위 → 사용자 흐름 → 개발 순서 → 기술과 구현` 순서로 진행한다.
- 프로젝트 이름만 보고 기능이나 목적을 추측하지 않는다.
- 결과에 큰 영향을 주는 제품 결정이 확정되지 않았다면 한 번에 하나씩 질문하고 사용자 확인을 받는다.
- 사용자 답변을 받으면 다음 질문이나 문서 반영 전에 확정된 사실, 작업을 위한 가정, 아직 결정하지 않은 내용을 구분한다.
- 기술 선택은 제품 범위와 사용자 흐름을 먼저 확인한 뒤 진행한다.
- 외부 서비스의 API, 정책, 지원 범위처럼 바뀔 수 있는 정보는 공식 1차 출처로 확인하고 확인한 사실과 추론을 구분한다.

## 정보의 책임과 충돌 처리

- `AGENTS.md`는 작업 방식, Issue는 해당 작업의 범위와 완료 조건, 제품 문서는 현재 제품 정의, 코드와 테스트는 구현된 동작을 책임진다.
- 제품 작업을 시작하기 전에 `docs/project-context.md`와 관련 제품 문서를 확인한다.
- 사용자 결정, Issue, 제품 문서, 코드와 테스트가 충돌하면 임의로 하나를 선택하지 않는다. 충돌과 영향을 설명하고 사용자 확인을 받는다.
- 결정이 바뀌면 해당 내용을 책임지는 문서와 영향을 받는 코드·테스트를 함께 갱신한다.

## 학습 중심 설명

- 핵심 결정은 `해결할 문제 → 핵심 원리 → 선택 가능한 방법 → 현재 방법을 선택한 이유 → 실제 변경 → 검증 방법과 주의사항` 순서로 설명한다.
- 선택하지 않은 방법의 장단점과 현재 선택의 트레이드오프를 함께 설명한다.
- 설명할 내용은 `현재 작업에 반드시 필요`, `알아두면 좋음`, `지금은 생략`으로 구분한다.
- 반복적인 보일러플레이트는 직접 처리하되 핵심 로직과 중요한 설계 결정은 사용자가 이해하고 판단할 수 있게 설명한다.
- 현재 목적에 충분한 깊이에 도달하면 학습을 멈출 지점을 알리고 다음 단계로 넘어간다.

## 문서별 책임

- `README.md`: 프로젝트 한 문장 소개와 핵심 문서의 진입점만 제공한다.
- `docs/project-context.md`: 해결할 문제, 대상 사용자, 핵심 가치와 제품 원칙을 설명한다.
- `docs/product/scope.md`: 현재 MVP의 포함 범위, 제외 범위, 성공 기준과 구현 전 결정할 정책을 정의한다.
- `docs/product/user-flow.md`: 사용자의 실제 행동 순서와 서비스의 예외 처리를 설명한다.
- `docs/product/development-plan.md`: 마일스톤별 개발 순서와 완료 조건을 정의한다.

같은 내용을 여러 문서에 반복하지 않는다. 세부 내용은 책임 문서 한 곳에 두고 다른 문서에서는 링크로 연결한다.

## 제품 문서 작성 원칙

- 제품 문서에는 현재 유효한 정의와 제약만 남긴다.
- 검토했지만 폐기한 후보, 대화 과정, 숫자를 맞추기 위한 고민과 임시 메모는 삭제한다.
- 제외 범위는 다음 개발자가 실수로 범위를 확장할 가능성이 있거나, 사용자 경험과 데이터 범위를 명확히 제한하는 항목만 기록한다.
- 선택되지 않은 대안과 의사결정 과정이 꼭 필요하면 제품 문서가 아니라 Issue, PR 또는 별도의 ADR에 기록한다.
- `현재 문서는`, `초안 상태`, `사용자 확인 후`, `처음에는`처럼 곧 낡는 작업 상태 문구를 제품 문서에 남기지 않는다.
- 날짜는 법률, 가격, 외부 서비스 지원 상태처럼 시점이 정확성에 직접 영향을 줄 때만 기록한다.
- 현재 동작을 긍정문으로 먼저 설명한다. 폐기한 동작을 부정문으로 나열해 현재 기능을 설명하지 않는다.
- 미확정 사항은 본문에 섞지 않고 `구현 전 결정할 정책`처럼 용도가 분명한 구역에만 둔다.
- 중요한 제품 결정은 문서에 반영하기 전에 사용자 확인을 받는다.

## 문서 검수 체크리스트

문서를 완료하기 전에 다음 항목을 직접 확인한다.

1. 이 문장이 현재 제품의 동작, 범위 또는 제약을 설명하는가?
2. 이 문장을 삭제해도 현재 제품을 동일하게 구현할 수 있다면 불필요한 의사결정 흔적은 아닌가?
3. 폐기한 후보나 선택 이유를 현재 요구사항처럼 기록하지 않았는가?
4. 다른 문서와 중복되거나 모순되는 내용이 없는가?
5. 임시 상태, 과거 시점과 작성 과정에 의존하는 표현이 없는가?
6. 미확정 사항을 확정된 사실처럼 쓰지 않았는가?
7. 모든 로컬 링크가 유효하고 Markdown 형식 오류가 없는가?

다음 표현을 검색해 문맥상 필요한지 특히 검토한다.

- `처음에는`, `검토했지만`, `합의한`, `초안`, `현재 문서는`
- `임의로`, `숫자를 맞추기`, `사용자 확인 후`, `나중에 결정`
- 특정 후보가 `제외되었다`, `탈락했다`, `대신 선택했다`는 설명

검색 결과가 있다고 무조건 삭제하지는 않는다. 현재 범위를 지키는 데 필요한 제약인지 먼저 판단한다.

## 구현 원칙

- YAGNI, KISS, DRY를 따른다.
- 현재 문제를 해결하지 않는 기술, 문서와 추상화를 미리 추가하지 않는다.
- 새로운 구조나 도구를 도입할 때 해결할 문제와 선택 이유를 설명한다.
- 새로운 의존성을 추가하기 전에 기존 코드나 플랫폼 기능으로 해결할 수 있는지 확인하고 도입 비용과 대안을 설명한다.
- 프로젝트의 기존 명명, 타입, 구조와 스타일을 우선한다.
- 사용자 화면은 정상 상태뿐 아니라 로딩, 빈 결과, 오류와 비활성 상태를 함께 설계한다.
- 프론트엔드 구현은 시맨틱 HTML, 연결된 레이블, 키보드 조작과 자연스러운 포커스 이동을 기본으로 확인한다.

## 데이터와 보안

- 인증 정보, API 키와 비밀값을 코드, 문서, fixture, 로그와 커밋에 포함하지 않는다.
- 실제 Steam 계정 식별자와 비공개 라이브러리 정보 등 개인 데이터는 개발 예시나 테스트 데이터로 사용하지 않는다.
- fixture와 문서 예시는 실제 사용자를 식별할 수 없는 합성 데이터를 사용한다.
- 외부 리뷰나 콘텐츠를 저장소에 추가할 때는 필요한 최소 범위만 사용하고 출처와 이용 조건을 확인한다.

## 완료 및 검증 원칙

- 완료를 보고하기 전에 변경 범위에 맞는 테스트, 타입 검사, 포맷 검사, 문서 링크 검사를 실행한다.
- 실행할 수 없는 검증이 있다면 생략한 항목과 이유, 남은 위험을 명시한다.
- 공통 용어, 상태, 사용자 흐름을 변경하면 저장소 전체를 검색해 관련 문서, 코드와 테스트를 함께 확인한다.
- 관련 이슈의 완료 조건을 충족하지 못했다면 작업을 완료했다고 표현하지 않는다.
- 변경 결과와 함께 확인 방법, 남아 있는 위험과 후속 작업을 보고한다.

## Git 작업 원칙

- 하나의 Issue는 하나의 브랜치와 PR로 처리하며 관련 없는 변경을 섞지 않는다.
- 이슈 작업은 해당 이슈를 위한 브랜치에서 진행하고 기본 브랜치에 직접 커밋하지 않는다.
- 사용자의 기존 변경과 stash를 임의로 적용, 삭제 또는 덮어쓰지 않는다.
- 작업 트리가 섞여 있다면 대상 파일만 경로로 지정해 스테이징하고 `git add .` 또는 `git add -A`를 사용하지 않는다.
- 커밋과 PR을 만들기 전에 변경 요약과 PR 본문 초안을 사용자에게 먼저 보여준다.
- 사용자 승인 없이 커밋, 푸시 또는 PR 생성을 진행하지 않는다.
