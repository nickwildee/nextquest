{
  "name": "nextquest",
  "private": true,
  "packageManager": "pnpm@10.23.0",
  "devDependencies": {
    "turbo": "latest"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "prettier --write ."
  }
}
